# v1.4.0-P5-2: ILanguageG2PHandler 導入準備（中間リファクタリング）

**マイルストーン**: v1.4.0 Phase 5（API改善・拡張性向上）
**優先度**: P2
**ステータス**: 完了
**見積もり**: 1人日（実装0.5日 + テスト確認0.5日）
**依存チケット**: なし（MS3-1非依存）
**後続チケット**: なし

---

## 1. タスク目的とゴール

`MultilingualPhonemizer.PhonemizeWithProsodyAsync()` 内の言語分岐（L330-534）は現在 if-else チェーンで7言語分が直列に並んでおり、各ブランチが30-140行の処理を含んでいる。v1.4.0 では private メソッド抽出 + switch 式化を行い、v2.0 で予定している Strategy パターン（`ILanguageG2PHandler`）への移行を準備する。

**v1.4.0 スコープ**: 振る舞い不変のリファクタリング（外部API変更なし）

**ゴール**:
- `PhonemizeWithProsodyAsync` の if-else チェーンが switch 式 + private メソッド呼び出しに置換されている
- 各言語の `ProcessXxx` メソッドが独立して存在する
- 外部API変更なし
- 全7言語の既存テストが変更なしでパス

---

## 2. 実装する内容の詳細

### 2.1 抽出するメソッド一覧

**ファイル**: `Assets/uPiper/Runtime/Core/Phonemizers/Multilingual/MultilingualPhonemizer.cs`

以下の7メソッドを `PhonemizeWithProsodyAsync` から抽出する:

| メソッド名 | 対象行 | 行数 | 複雑度 | 備考 |
|-----------|--------|------|--------|------|
| `ProcessJapanese` | L330-346 | 17行 | 低 | 同期処理。PAD先頭除去あり |
| `ProcessEnglish` | L347-366 | 20行 | 低 | 同期処理。ToPuaPhonemes + ToIpaWithProsody |
| `ProcessSpanish` | L367-374 | 8行 | 低 | 同期処理。ExtractProsodyArrays使用 |
| `ProcessFrench` | L375-382 | 8行 | 低 | 同期処理。ToPuaPhonemes + ExtractProsodyArrays |
| `ProcessPortuguese` | L383-390 | 8行 | 低 | 同期処理。FR と同一パターン |
| `ProcessChinese` | L391-467 | 77行 | **高** | 同期処理。トーンPUA挿入、非均等音節分配、残余音素処理 |
| `ProcessKorean` | L468-514 | 47行 | 中 | 同期処理。PUA/Prosody長不一致フォールバック |

### 2.2 戻り値型の統一

全 `ProcessXxx` メソッドの戻り値型をタプルで統一する:

```csharp
private (string[] phonemes, int[] a1, int[] a2, int[] a3) ProcessXxx(string text)
```

各メソッドは同期メソッドとして定義する（全7言語が同期処理のため）。

### 2.3 switch 式への置換

**変更前（L329-534）**: if-else チェーン

```csharp
if (lang == "ja" && _jaPhonemizer != null)
{
    // ... 17行
}
else if (lang == "en" && _enEngine != null)
{
    // ... 20行
}
// ... 5言語分
else
{
    // フォールバック（async）
}
```

**変更後**:

```csharp
// Phonemize each segment
(string[] segPhonemes, int[] segA1, int[] segA2, int[] segA3) result;
bool processed = true;

switch (lang)
{
    case "ja" when _jaPhonemizer != null:
        result = ProcessJapanese(segText);
        break;
    case "en" when _enEngine != null:
        result = ProcessEnglish(segText);
        break;
    case "es" when _esEngine != null:
        result = ProcessSpanish(segText);
        break;
    case "fr" when _frEngine != null:
        result = ProcessFrench(segText);
        break;
    case "pt" when _ptEngine != null:
        result = ProcessPortuguese(segText);
        break;
    case "zh" when _zhEngine != null:
        result = ProcessChinese(segText);
        break;
    case "ko" when _koG2PEngine != null:
        result = ProcessKorean(segText);
        break;
    default:
        // Async fallback path: legacy backend
        var fallbackResult = await ProcessFallbackAsync(lang, segText, cancellationToken);
        if (fallbackResult.phonemes == null)
            continue;
        result = fallbackResult;
        break;
}

segPhonemes = result.phonemes;
segA1 = result.a1;
segA2 = result.a2;
segA3 = result.a3;
```

> **注意**: C# の switch 式 (`=>` 構文) は `await` を含む default ケースで使えないため、switch 文 (`case:` 構文) を使用する。

### 2.4 各 ProcessXxx メソッドの詳細

**(a) ProcessJapanese（L330-346）**:

```csharp
private (string[] phonemes, int[] a1, int[] a2, int[] a3) ProcessJapanese(string text)
{
    var result = _jaPhonemizer.PhonemizeWithProsody(text);
    var phonemes = result.Phonemes ?? Array.Empty<string>();
    var a1 = result.ProsodyA1 ?? Array.Empty<int>();
    var a2 = result.ProsodyA2 ?? Array.Empty<int>();
    var a3 = result.ProsodyA3 ?? Array.Empty<int>();

    // Strip leading PAD ("_") from Japanese segments (added from "sil" conversion)
    if (phonemes.Length > 0 && phonemes[0] == "_")
    {
        phonemes = phonemes[1..];
        a1 = a1.Length > 1 ? a1[1..] : a1;
        a2 = a2.Length > 1 ? a2[1..] : a2;
        a3 = a3.Length > 1 ? a3[1..] : a3;
    }

    return (phonemes, a1, a2, a3);
}
```

**(b) ProcessEnglish（L347-366）**:

```csharp
private (string[] phonemes, int[] a1, int[] a2, int[] a3) ProcessEnglish(string text)
{
    var prosodyResult = _enEngine.ToIpaWithProsody(text);
    var phonemes = _enEngine.ToPuaPhonemes(text);
    var prosody = prosodyResult.Prosody;
    var a1 = new int[phonemes.Length];
    var a2 = new int[phonemes.Length];
    var a3 = new int[phonemes.Length];
    for (var i = 0; i < phonemes.Length && i < prosody.Length; i++)
    {
        a1[i] = prosody[i].A1;
        a2[i] = prosody[i].A2;
        a3[i] = prosody[i].A3;
    }
    return (phonemes, a1, a2, a3);
}
```

**(c) ProcessSpanish（L367-374）**:

```csharp
private (string[] phonemes, int[] a1, int[] a2, int[] a3) ProcessSpanish(string text)
{
    var result = _esEngine.ToIpaWithProsody(text);
    var phonemes = result.Phonemes ?? Array.Empty<string>();
    var (a1, a2, a3) = ExtractProsodyArrays(
        result.Prosody, p => (p.A1, p.A2, p.A3), phonemes.Length);
    return (phonemes, a1, a2, a3);
}
```

**(d) ProcessFrench（L375-382）**:

```csharp
private (string[] phonemes, int[] a1, int[] a2, int[] a3) ProcessFrench(string text)
{
    var phonemes = _frEngine.ToPuaPhonemes(text);
    var result = _frEngine.ToIpaWithProsody(text);
    var (a1, a2, a3) = ExtractProsodyArrays(
        result.Prosody, p => (p.A1, p.A2, p.A3), phonemes.Length);
    return (phonemes, a1, a2, a3);
}
```

**(e) ProcessPortuguese（L383-390）**:

```csharp
private (string[] phonemes, int[] a1, int[] a2, int[] a3) ProcessPortuguese(string text)
{
    var phonemes = _ptEngine.ToPuaPhonemes(text);
    var result = _ptEngine.ToIpaWithProsody(text);
    var (a1, a2, a3) = ExtractProsodyArrays(
        result.Prosody, p => (p.A1, p.A2, p.A3), phonemes.Length);
    return (phonemes, a1, a2, a3);
}
```

**(f) ProcessChinese（L391-467）**:

最も複雑なブランチ。`TonePuaChars`（L52, static readonly）への参照は問題なし。

```csharp
private (string[] phonemes, int[] a1, int[] a2, int[] a3) ProcessChinese(string text)
{
    var puaPhonemes = _zhEngine.ToPuaPhonemes(text);
    var prosodyResult = _zhEngine.ToIpaWithProsody(text);

    var phonemeList = new List<string>();
    var prosodyA1List = new List<int>();
    var prosodyA2List = new List<int>();
    var prosodyA3List = new List<int>();

    int totalSyllables = prosodyResult.Prosody.Count;
    if (totalSyllables == 0)
    {
        foreach (var p in puaPhonemes)
            phonemeList.Add(p);
    }
    else
    {
        // Distribute PUA phonemes across syllables and insert tone markers
        int phonemesPerSyllable = puaPhonemes.Length / totalSyllables;
        int remainder = puaPhonemes.Length % totalSyllables;
        int puaIdx = 0;

        for (int syl = 0; syl < totalSyllables; syl++)
        {
            // ... 音節分配・トーンマーカー挿入ロジック（既存コードそのまま）
        }

        // Add any remaining phonemes
        for (; puaIdx < puaPhonemes.Length; puaIdx++)
        {
            // ... 残余音素処理（既存コードそのまま）
        }
    }

    var phonemes = phonemeList.ToArray();
    var segA1 = prosodyA1List.ToArray();
    var segA2 = prosodyA2List.ToArray();
    var segA3 = prosodyA3List.ToArray();

    // Ensure prosody arrays are aligned with phoneme count
    if (segA1.Length < phonemes.Length)
    {
        Array.Resize(ref segA1, phonemes.Length);
        Array.Resize(ref segA2, phonemes.Length);
        Array.Resize(ref segA3, phonemes.Length);
    }

    return (phonemes, segA1, segA2, segA3);
}
```

**(g) ProcessKorean（L468-514）**:

```csharp
private (string[] phonemes, int[] a1, int[] a2, int[] a3) ProcessKorean(string text)
{
    var puaPhonemes = _koG2PEngine.ToPuaPhonemes(text);
    var prosodyResult = _koG2PEngine.ToIpaWithProsody(text);
    var phonemes = puaPhonemes ?? Array.Empty<string>();

    int[] segA1, segA2, segA3;

    if (prosodyResult.Prosody.Length == phonemes.Length)
    {
        // 1:1 対応パス
        segA1 = new int[phonemes.Length];
        segA2 = new int[phonemes.Length];
        segA3 = new int[phonemes.Length];
        for (var i = 0; i < prosodyResult.Prosody.Length; i++)
        {
            segA1[i] = prosodyResult.Prosody[i].A1;
            segA2[i] = prosodyResult.Prosody[i].A2;
            segA3[i] = prosodyResult.Prosody[i].A3;
        }
    }
    else
    {
        // フォールバック: 長不一致
        segA1 = new int[phonemes.Length];
        segA2 = new int[phonemes.Length];
        segA3 = new int[phonemes.Length];
        if (prosodyResult.Prosody.Length > 0)
        {
            var defaultA3 = prosodyResult.Prosody[0].A3;
            for (var i = 0; i < phonemes.Length; i++)
            {
                segA3[i] = i < prosodyResult.Prosody.Length
                    ? prosodyResult.Prosody[i].A3
                    : defaultA3;
            }
        }
        PiperLogger.LogWarning(
            $"[MultilingualPhonemizer] Korean PUA/prosody length mismatch: " +
            $"PUA={phonemes.Length}, Prosody={prosodyResult.Prosody.Length}");
    }

    return (phonemes, segA1, segA2, segA3);
}
```

### 2.5 フォールバックブランチの抽出

**変更前（L515-534）**: else ブロック（async メソッド `backend.PhonemizeAsync()` を呼び出し）

**変更後**:

```csharp
private async Task<(string[] phonemes, int[] a1, int[] a2, int[] a3)> ProcessFallbackAsync(
    string lang, string text, CancellationToken cancellationToken)
{
    var backend = GetBackendForLanguage(lang);
    if (backend != null)
    {
        var result = await backend.PhonemizeAsync(text, lang, null, cancellationToken);
        var phonemes = result?.Phonemes ?? Array.Empty<string>();
        var a1 = result?.ProsodyA1 ?? new int[phonemes.Length];
        var a2 = result?.ProsodyA2 ?? new int[phonemes.Length];
        var a3 = result?.ProsodyA3 ?? new int[phonemes.Length];
        return (phonemes, a1, a2, a3);
    }

    PiperLogger.LogWarning(
        $"[MultilingualPhonemizer] No backend for '{lang}', skipping segment.");
    return (null, null, null, null);
}
```

フォールバックのみ async メソッドとして分離する。呼び出し元の switch 文 default ケースで `await` する。`ProcessFallbackAsync` が `(null, null, null, null)` を返した場合は `continue` でスキップする。

---

## 3. 受け入れ条件

- [x] `PhonemizeWithProsodyAsync` の if-else チェーン（L330-534）が switch 文に置換されている
- [x] 7つの `ProcessXxx` メソッド + 1つの `ProcessFallbackAsync` メソッドが存在する
- [x] 各 `ProcessXxx` メソッドの戻り値型が `(string[] phonemes, int[] a1, int[] a2, int[] a3)` で統一されている
- [x] 外部APIに変更なし（`PhonemizeWithProsodyAsync` のシグネチャ・戻り値型は不変）
- [x] 全7言語の既存テストが変更なしでパス
- [x] `dotnet format --verify-no-changes` がパス

---

## 4. 提供範囲・テスト項目

### 提供範囲 (Scope)

**In-scope**:
- `PhonemizeWithProsodyAsync` の if-else → switch 文化
- 7言語分の `ProcessXxx` private メソッド抽出
- `ProcessFallbackAsync` の async メソッド抽出
- 戻り値型のタプル統一

**Out-of-scope**:
- `ILanguageG2PHandler` インターフェースの定義（v2.0）
- `Dictionary<string, ILanguageG2PHandler>` ベースのハンドラー登録（v2.0）
- `InitializeAsync` のリファクタリング
- 各 `ProcessXxx` メソッドの public 化

### テスト項目

| テストケース | 内容 | 検証ポイント |
|------------|------|------------|
| 日本語テスト群 | `MultilingualPhonemizerTests` の JA テスト | `ProcessJapanese` の振る舞い不変 |
| 英語テスト群 | EN セグメントテスト | `ProcessEnglish` の振る舞い不変 |
| スペイン語テスト群 | `MultilingualPhonemizerDeepTests` の ES テスト | `ProcessSpanish` の振る舞い不変 |
| フランス語テスト群 | FR テスト | `ProcessFrench` の振る舞い不変 |
| ポルトガル語テスト群 | PT テスト | `ProcessPortuguese` の振る舞い不変 |
| 中国語テスト群 | `ChinesePhonemizerTests` + Deep テスト | `ProcessChinese` の振る舞い不変（トーンPUA挿入含む） |
| 韓国語テスト群 | KO テスト | `ProcessKorean` の振る舞い不変 |
| 多言語混在テスト | `SegmentText_MixedText_*` | 複数 `ProcessXxx` の連結が正しい |
| EOS処理テスト | `MultilingualPhonemizerEosTests` | switch後のEOS除去ロジックが正常動作 |
| フォールバックテスト | `ProcessFallbackAsync` パス | レガシーバックエンド経由の動作維持 |

---

## 5. 懸念事項・レビュー項目

### 5.1 中国語処理の変数スコープ整理

`ProcessChinese` は最も複雑で、以下の変数を内部で使用する:
- `puaPhonemes`（`string[]`）: `_zhEngine.ToPuaPhonemes()` の結果
- `prosodyResult`: `_zhEngine.ToIpaWithProsody()` の結果
- `phonemeList`, `prosodyA1List`, `prosodyA2List`, `prosodyA3List`（`List<T>`）: 動的構築用
- `totalSyllables`, `phonemesPerSyllable`, `remainder`, `puaIdx`: ループ制御変数
- `TonePuaChars`（static readonly, L52）: トーンPUA文字配列

全変数が `ProcessChinese` メソッド内にローカルスコープとして収まるため、変数スコープの問題は発生しない。`TonePuaChars` は static フィールドのため、private メソッドからも問題なくアクセス可能。

### 5.2 フォールバック分岐の async 混在

7言語の `ProcessXxx` は全て同期メソッドだが、`ProcessFallbackAsync` のみ async メソッドである。switch 文の default ケースで `await` を使用するため、`PhonemizeWithProsodyAsync` メソッド自体は引き続き async メソッドとして維持される。

**注意**: フォールバックが `(null, null, null, null)` を返した場合に `continue` でスキップする処理は、switch 文の後（既存の `if (segPhonemes.Length == 0) continue;` の前）に null チェックを追加する必要がある:

```csharp
if (result.phonemes == null)
    continue;
```

### 5.3 switch 文の when ガード

`case "ja" when _jaPhonemizer != null:` のような when ガードを使用する。when 条件に一致しない場合は default ケースにフォールスルーし、`ProcessFallbackAsync` が呼ばれる。これは既存の if-else チェーンの else ブロック（L515-534）と同一の動作。

### 5.4 `NotSupportedException` の不使用

switch 文の default ケースで `NotSupportedException` をスローすると、現在のフォールバック動作（レガシーバックエンドへの委譲）が失われる。default ケースは必ず `ProcessFallbackAsync` を呼び出し、既存のフォールバック動作を維持する。

### 5.5 レビュー重点項目

- [ ] 各 `ProcessXxx` メソッドの出力が既存 if-else ブランチと完全に同一か（diff で確認）
- [ ] switch 文の when ガード条件が既存の if 条件と一致しているか
- [ ] `ProcessChinese` の `Array.Resize` 呼び出しが正しく戻り値に反映されているか（ref パラメータ注意）
- [ ] `ProcessFallbackAsync` の null 戻り値が呼び出し元で正しくハンドリングされているか
- [ ] EOS除去ロジック（L539-552）が switch 文化後も正しく動作するか
- [ ] `ProcessKorean` のログ出力（`PiperLogger.LogWarning`）が維持されているか

## 実装メモ（2026-04-06完了）

`MultilingualPhonemizer.cs` の `PhonemizeWithProsodyAsync` を以下のようにリファクタリング:

- `PhonemizeWithProsodyAsync` 内のif-elseチェーン（約200行）→ `switch` 文に置換
- 抽出したprivateメソッド: `ProcessJapanese`, `ProcessEnglish`, `ProcessSpanish`,
  `ProcessFrench`, `ProcessPortuguese`, `ProcessChinese`, `ProcessKorean`
- 非同期フォールバック: `ProcessFallbackAsync`
- 外部API変更なし（振る舞い不変のリファクタリング）
